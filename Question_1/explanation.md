# Command Explanations - Question 1

- `whoami`: I executed this command to display the current logged-in username, verifying the account in use as `sharun`.
- `id`: I used this command to display the current user ID, group ID, and group memberships for the `sharun` account.
- `groups`: I ran this command to list all groups associated with my account, confirming membership in `adm`, `cdrom`, `sudo`, `dip`, `plugdev`, and `users`.
- `echo $SHELL`: I printed this environment variable to confirm which shell is active, verifying it is set to `/bin/bash`.
- `pwd`: I used the print working directory command to confirm my current location in the file system as `/home/sharun`.
- `ls -la`: I executed this to list all visible and hidden files in my home directory, showing configuration files and project folders.
- `ping -c 4 google.com`: I ran a four-packet ping to `google.com` to verify external network connectivity, confirming successful packet transmission and receipt with 0% packet loss.
